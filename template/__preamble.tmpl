# Preamble

This file only provides template directives; it is skipped for the actual output.

{{skip "__preamble"}}

{{if not (eq .cicd_provider "github")}}
  {{skip "{{.project_name}}/.github"}}
{{end}}

{{if not (eq .cicd_provider "azure")}}
  {{skip "{{.project_name}}/.azuredevops"}}
{{end}}

{{if not (eq .include_example_jobs "yes")}}
  {{skip "{{.project_name}}/notebooks/example_notebook.py"}}
  {{skip "{{.project_name}}/notebooks/demo_dlt_pipeline_notebook.py"}}
  {{skip "{{.project_name}}/resources/example_workflow.yml"}}
  {{skip "{{.project_name}}/resources/demo_dlt_pipeline.yml"}}
  {{skip "{{.project_name}}/sql/demo_dlt_pipeline_file.sql"}}
{{end}}

{{if not (eq .use_devcontainer "yes")}}
  {{skip "{{.project_name}}/.devcontainer"}}
{{end}}
